version: '2'
services:
  server:
    build: ./server
    restart: always
    network_mode: host
    ports:
      - "3000:3000"
  greeter-service:
    build: ./greeter-service
    restart: always
    network_mode: host
  adder-service:
    build: ./adder-service
    restart: always
    network_mode: host